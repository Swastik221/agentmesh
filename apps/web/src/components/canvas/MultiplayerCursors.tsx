/**
 * Renders collaborator cursors on the canvas when real position coordinates are present.
 * Renders nothing when no real remote collaborators are active.
 */
export function MultiplayerCursors() {
  return null;
}
